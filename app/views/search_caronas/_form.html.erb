<%= form_with(model: search_carona) do |form| %>
  <% if search_carona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_carona.errors.count, "error") %> prohibited this search_carona from being saved:</h2>

      <ul>
        <% search_carona.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :departure %>
    <%= form.text_field :departure %>
  </div>

  <div class="field">
    <%= form.label :arrival %>
    <%= form.text_field :arrival %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
